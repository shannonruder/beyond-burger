<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}Pick up!{{else}}Devour It!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">delete!</button>
</li>
